spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}

  mail:
    host: ${SPRING_MAIL_HOST}
    port: ${SPRING_MAIL_PORT}
    username: ${SPRING_MAIL_USERNAME}
    password: ${SPRING_MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: ${SPRING_MAIL_SMTP_AUTH:true}
          starttls:
            enable: ${SPRING_MAIL_SMTP_STARTTLS_ENABLE:true}
      default-encoding: UTF-8

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true
    baseline-version: 1
calendly:
  client_id: ${CALENDLY_CLIENT_ID}
  redirect-uri: ${CALENDLY_REDIRECT_URI}
  client-secret: ${CALENDLY_CLIENT_SECRET}
  webhook-signing-key: ${CALENDLY_WEBHOOK_SIGNING_KEY}
  scope: organization.read scheduled_events.read scheduled_events.read_all

management:
  endpoints:
    web:
      exposure:
        include: health,prometheus
  endpoint:
    health:
      show-details: always
    prometheus:
      enabled: true

neuroforged:
  admin:
    email: ${NEUROFORGED_ADMIN_EMAIL}
    password: ${NEUROFORGED_ADMIN_PASSWORD}
  jwt:
    secret: ${NEUROFORGED_JWT_SECRET}
  cookie:
    # LSB-159: blank in local — browsers keep host-only cookies for localhost/127.0.0.1
    domain: ""
  cors:
    allowed-origins: "http://localhost:3000,http://localhost:5173"
  mail:
    from: ${SPRING_MAIL_FROM}
  tokens:
    internal: ${NEUROFORGED_INTERNAL_TOKEN}
  encryption-key: ${NEUROFORGED_ENCRYPTION_KEY:}
  seed-sample-data: false

rate-limit:
  requests-per-minute: 60
  requests-per-hour: 500

scraper:
  api-url: ${SCRAPER_API_URL:https://scraper.alchemizeiq.com}
  api-key: ${SCRAPER_API_KEY}

logging:
  level:
    com.neuroforged.leadsystem: DEBUG
    org.springframework: INFO
    org.springframework.security: WARN
    org.hibernate.SQL: WARN
    org.hibernate.orm.jdbc.bind: WARN
    com.zaxxer.hikari: WARN
    # Suppress actuator health check request noise
    org.springframework.web.servlet.DispatcherServlet: WARN
